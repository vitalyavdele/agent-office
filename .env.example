# URL дашборда (FastAPI), доступный из n8n
# Если n8n и дашборд на одной машине — можно http://localhost:8000
DASHBOARD_URL=http://localhost:8000

# Webhook URL Manager-воркфлоу в n8n
# После импорта n8n/manager.json и активации — скопируй URL из узла Webhook
N8N_MANAGER_WEBHOOK=http://localhost:5678/webhook/manager

# База URL для воркер-воркфлоу (manager.json достучится до каждого по /<agent>)
# Пример: http://localhost:5678/webhook/worker → нужны отдельные воркфлоу со своими путями
N8N_WORKER_WEBHOOK_BASE=http://localhost:5678/webhook/worker
